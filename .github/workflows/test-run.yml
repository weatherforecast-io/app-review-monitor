name: Test Run (Manual)

on:
  workflow_dispatch: {}

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run review check (no state - fetches recent reviews)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SLACK_WEBHOOK_URLS: ${{ vars.SLACK_WEBHOOK_URLS }}
          APP_IDS: ${{ vars.APP_IDS }}
          APP_NAMES: ${{ vars.APP_NAMES }}
          APP_COUNTRIES: ${{ vars.APP_COUNTRIES }}
        run: python main.py
